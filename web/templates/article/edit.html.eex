<h4>記事編集</h4>

<%= render "form.html", changeset: @changeset,
                        categories: @categories,
                        action: article_path(@conn, :update, @article) %>

<%= link "一覧へ", to: article_path(@conn, :index) %>
